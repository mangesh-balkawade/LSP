#include <stdio.h>

int main(int argc, char *argv[])
{
    printf("Executable no %s \n", argv[0]);
    printf("server url no %s \n", argv[1]);
    printf("post no %s \n", argv[2]);
    printf("required file %s \n", argv[3]);
    printf("file to save %s \n", argv[4]);
    return 0;
}